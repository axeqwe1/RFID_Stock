export const useOutstock = () => {
  // Define any reactive state, computed properties, or methods related to Outstock here

  return {
    // Return the state, computed properties, and methods to be used in components
  };
};
